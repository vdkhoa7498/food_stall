import {socket} from '.';

export const socketEvents = ({value, setValue}) => {
  // get all user online


  // socket.on('listentonewmessage', (data) => {
  //   if (data !== undefined) {
  //     store.dispatch(getNewMessage(data));
  //   }
  // });

  // // get all user online
  // socket.on("listentonewroom", (data) => {
  //   if (data !== undefined) {
  //     store.dispatch(getNewRoomFromAnotherClient(data));
  //   }
  // });

};
